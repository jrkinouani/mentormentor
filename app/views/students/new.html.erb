<div class="container">
	<h1>Vous cherchez un mentor ?</h1>
	

	<%= form_for @student do |f|%>
	<%= f.email_field :email %>
	<%= f.select :cours, ['France', 'Belgique', 'Luxembourg', 'Suisse'] %>
	<%= f.text_field :city %>
	<%= f.submit %> 
  </div>
	<% end %>
</div>